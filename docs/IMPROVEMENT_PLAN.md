# Структурированный план доработок UWS

**Дата создания:** 2026-02-07  
**Статус:** На согласовании  
**Приоритет:** По степени критичности (Критические → Важные → Рекомендуемые → Опциональные)

---

## 🔴 КАТЕГОРИЯ 1: Критические исправления

### 1.1 API Endpoints (Критический дефект)

**Описание проблемы:**
API endpoints возвращают 404 ошибки:
- `/api/telegram` - отсутствует
- `/api/orders` - отсутствует

**Обоснование:**
Формы заявок не могут отправлять данные, что блокирует основной бизнес-процесс.

**Текущее состояние:**
- `src/components/forms/OrderForm.tsx:99-105` - вызов `/api/telegram`
- `src/pages/contacts.astro:319` - вызов `/api/orders`

**Рекомендация:**
1. Создать `functions/api/telegram.ts` с обработкой Telegram Bot API
2. Создать `functions/api/orders.ts` с сохранением в Supabase
3. Добавить валидацию входных данных
4. Настроить обработку ошибок

**Трудоёмкость:** 4-6 часов  
**Результат:** Работающие API endpoints

---

### 1.2 Отсутствующие изображения (Критический дефект)

**Описание проблемы:**
Lighthouse выявил 404 ошибки для изображений:
- `public/images/testimonials/testimonials-1.jpg` ... `testimonials-6.jpg`
- `public/favicon.svg`

**Обоснование:**
Влияет на производительность и SEO (ошибки в консоли).

**Текущее состояние:**
- `src/components/blocks/Testimonials.astro` - использует отсутствующие изображения

**Рекомендация:**
1. Добавить placeholder изображения или создать тестовые изображения
2. Настроить `astro.config.mjs` для fallback изображений

**Трудоёмкость:** 1-2 часа  
**Результат:** Отсутствие 404 ошибок

---

### 1.3 Конфигурация Supabase (Критический дефект)

**Описание проблемы:**
Supabase не настроен в соответствии с документацией `docs/supabase-migration.sql`

**Обоснование:**
Хранение заявок и история заказов недоступны.

**Текущее состояние:**
- `.env.example` содержит шаблон, но `.env` не настроен
- Таблицы не созданы в Supabase

**Рекомендация:**
1. Создать `.env` с реальными значениями Supabase
2. Выполнить миграцию `docs/supabase-migration.sql`
3. Проверить подключение

**Трудоёмкость:** 2-3 часа  
**Результат:** Работающее подключение к Supabase

---

## 🟠 КАТЕГОРИЯ 2: Важные доработки

### 2.1 Конфигурация Telegram Bot (Важно)

**Описание проблемы:**
Telegram бот не настроен в соответствии с документацией.

**Обоснование:**
Уведомления о новых заявках не отправляются в Telegram.

**Текущее состояние:**
- `functions/api/telegram.ts` не создан

**Рекомендация:**
1. Добавить переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
2. Реализовать отправку сообщений через Bot API
3. Добавить логирование

**Трудоёмкость:** 2-3 часа  
**Результат:** Работающие уведомления в Telegram

---

### 2.2 Настройка Site URL (Важно)

**Описание проблемы:**
Несоответствие между документацией и конфигурацией.

**Обоснование:**
SEO и OpenGraph теги используют некорректный URL.

**Текущее состояние:**
- Документация: `https://uws.com.ua`
- Конфигурация: `https://tim21102025-source.github.io/UWS`

**Рекомендация:**
1. Обновить `astro.config.mjs` с корректным site URL
2. Проверить все SEO компоненты

**Трудоёмкость:** 0.5 часа  
**Результат:** Корректная конфигурация URL

---

### 2.3 Исправление ошибок Lighthouse (Важно)

**Описание проблемы:**
LCP = 4.0s при целевом ≤ 2.5s.

**Обоснование:**
Влияет на пользовательский опыт и SEO.

**Текущее состояние:**
- FCP = 2.0s (хорошо)
- LCP = 4.0s (требует оптимизации)
- TBT = 0ms (отлично)

**Рекомендация:**
1. Оптимизировать загрузку критических ресурсов
2. Добавить `loading="eager"` для LCP элементов
3. Настроить `astro.config.mjs` с оптимизацией изображений

**Трудоёмкость:** 2-3 часа  
**Результат:** LCP ≤ 2.5s

---

### 2.4 Удаление дублирующего кода (Важно)

**Описание проблемы:**
Дублирование компонентов:
- `components/business/WindowCalculator.tsx`
- `components/calculator/WindowCalculator.tsx`

**Обоснование:**
Усложняет поддержку кода.

**Текущее состояние:**
Два компонента калькулятора с возможно разной функциональностью.

**Рекомендация:**
1. Проанализировать оба компонента
2. Объединить функциональность
3. Удалить дубликат

**Трудоёмкость:** 2-4 часа  
**Результат:** Единый компонент калькулятора

---

## 🟢 КАТЕГОРИЯ 3: Рекомендуемые улучшения

### 3.1 Локализация (Рекомендуется)

**Описание проблемы:**
Текущая локализация ограничена редиректами.

**Обоснование:**
Для украинских пользователей необходим полноценный перевод.

**Текущее состояние:**
- `/uk/` и `/ru/` - только редиректы на главную

**Рекомендация:**
1. Создать `src/pages/uk/index.astro` с переводом
2. Создать `src/pages/ru/index.astro` с переводом
3. Обновить `src/i18n/config.ts` если существует

**Трудоёмкость:** 8-16 часов  
**Результат:** Полноценная поддержка локализации

---

### 3.2 Улучшение SEO (Рекомендуется)

**Описание проблемы:**
Не все SEO рекомендации реализованы.

**Обоснование:**
Влияет на позиции в поисковых системах.

**Текущее состояние:**
- Open Graph теги присутствуют
- Schema.org частично реализован

**Рекомендация:**
1. Добавить `sitemap.xml`
2. Добавить `robots.txt`
3. Расширить Schema.org разметку
4. Добавить канонические URL

**Трудоёмкость:** 4-6 часов  
**Результат:** Полноценное SEO

---

### 3.3 Улучшение пользовательского опыта (Рекомендуется)

**Описание проблемы:**
Обработка ошибок форм требует улучшения.

**Обоснование:**
Пользователи не получают понятную обратную связь.

**Текущее состояние:**
- Используется `alert()` для ошибок
- Нет toast-уведомлений

**Рекомендация:**
1. Заменить `alert()` на toast-уведомления
2. Добавить анимации загрузки
3. Улучшить валидацию форм в реальном времени

**Трудоёмкость:** 4-6 часов  
**Результат:** Улучшенный UX

---

## ⚪ КАТЕГОРИЯ 4: Опциональные оптимизации

### 4.1 Интеграция с аналитикой (Опционально)

**Описание проблемы:**
GA4 интеграция требует доработки.

**Текущее состояние:**
- `OrderForm.tsx` содержит GA4 tracking

**Рекомендация:**
1. Добавить полную GA4 интеграцию
2. Настроить события для всех форм
3. Добавить цели в Google Analytics

**Трудоёмкость:** 2-4 часа  
**Результат:** Полноценная аналитика

---

### 4.2 Performance Monitoring (Опционально)

**Описание проблемы:**
Нет мониторинга производительности в продакшене.

**Рекомендация:**
1. Интегрировать Sentry или аналог
2. Настроить tracking ошибок
3. Добавить performance monitoring

**Трудоёмкость:** 4-8 часов  
**Результат:** Мониторинг в реальном времени

---

## Сводная таблица приоритетов

| Приоритет | Задача | Трудоёмкость | Статус |
|-----------|--------|---------------|--------|
| 🔴 1.1 | API Endpoints | 4-6 ч | Ожидает |
| 🔴 1.2 | Изображения | 1-2 ч | Ожидает |
| 🔴 1.3 | Supabase | 2-3 ч | Ожидает |
| 🟠 2.1 | Telegram Bot | 2-3 ч | Ожидает |
| 🟠 2.2 | Site URL | 0.5 ч | Ожидает |
| 🟠 2.3 | LCP оптимизация | 2-3 ч | Ожидает |
| 🟠 2.4 | Дублирование кода | 2-4 ч | Ожидает |
| 🟢 3.1 | Локализация | 8-16 ч | Ожидает |
| 🟢 3.2 | SEO | 4-6 ч | Ожидает |
| 🟢 3.3 | UX улучшения | 4-6 ч | Ожидает |
| ⚪ 4.1 | Analytics | 2-4 ч | Ожидает |
| ⚪ 4.2 | Monitoring | 4-8 ч | Ожидает |

---

## Ожидаемые результаты

После выполнения всех задач:
- ✅ Работающие API endpoints (Telegram, Orders)
- ✅ Корректная конфигурация Supabase
- ✅ Отсутствие 404 ошибок
- ✅ LCP ≤ 2.5s
- ✅ Полноценная локализация
- ✅ Улучшенное SEO
- ✅ Лучший пользовательский опыт

---

**План готов к согласованию и реализации.**
